// initial value of reducer
export const initialMessage = "Reducer";

// reducer conditional function
// reducer backend function default var is state and action>type and payload
export const messReducer = (state, {type, payload}) => {
    return state = payload;
}